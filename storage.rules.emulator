// Emulator-only rules — admin SDK bypasses these entirely.
// This file is required by the Storage emulator but has no effect on tests.
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
