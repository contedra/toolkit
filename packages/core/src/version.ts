/**
 * Current content-model schema version shipped by `@contedra/core`.
 * Each release of the JSON Schemas (and the generated valibot module)
 * lives under `schemas/<SCHEMA_VERSION>/` and `generated/<SCHEMA_VERSION>/`.
 *
 * Bump alongside breaking changes to the schema; the value MUST equal
 * the directory name that ships the schemas in this release.
 */
export const SCHEMA_VERSION = "1.1.0";
